<script>
    $(document).ready(function () {
        var id = null;
        $("#marketselector li").each(function (i, elem) {
            $(elem).click(onClick);
        })

        function onClick() {
            id = $(this).attr("id");
            $("#marketId").val(id);
            $("#marketselector li").each(function (i, elem) {
                $(elem).removeClass("selected");
            });
            $(this).addClass("selected");
        }
    });
</script>
<ul class="selector" id="marketselector">
    {{#each markets}}
    <li id="{{this.id}}">{{this.name}}</li>
    {{/each}}
</ul>