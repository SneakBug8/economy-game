<script>
    $(document).ready(function () {
        var id = null;
        $("#{{inputId}}selector tr").each(function (i, elem) {
            $(elem).click(onClick);
        })

        function onClick() {
            id = $(this).attr("id");
            $("#{{inputId}}").val(id);
            $("#{{inputId}}selector tr").each(function (i, elem) {
                $(elem).removeClass("selected");
            });
            $(this).addClass("selected");
        }
    });
</script>

<div class="selectorcontainer">
<table id="{{inputId}}selector">
    <thead>
        <th>Название</th>
    </thead>
    {{#each goods}}
    <tr id="{{this.id}}">
        <td>{{this.name}} ({{this.id}})</td>
    </tr>
    {{/each}}
</table>
</div>